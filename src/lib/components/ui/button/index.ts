import Root, {
	type ButtonVariant,
	type ButtonProps,
	type ButtonSize,
	buttonVariants,
} from "./button.svelte";

export {
	type ButtonProps as Props,
	type ButtonVariant,
	type ButtonProps,
	type ButtonSize,
	//
	Root as Button,
	buttonVariants,
	Root,
};
